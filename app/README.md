# 问象

Flutter client for the local EleGit companion server. The phone only talks to that API (LAN or Cloudflare tunnel).

```bash
flutter pub get
flutter run
```

On a physical device, set the server URL to the PC LAN address or `https://*.trycloudflare.com` — not `localhost`.
