---
description: 问象仓库内改完即提交、重启服务、打包，无需再向用户确认
alwaysApply: true
---

# 自主交付（问象 EleGit）

在本仓库完成任务后，默认自动收尾，**不要**再向用户确认「要不要提交 / 重启 / 打包」。

## 何时做

| 动作 | 触发条件 |
|------|----------|
| **git commit + push** | 有 intentional 代码改动且任务已做完；commit message 写清 why；不提交 `.env` 和密钥 |
| **重启本机问象服务** | 改了 `server/`、根目录 `.env` 示例以外的服务配置、或 SSE/API 行为；目标是 `http://127.0.0.1:8787/health` 恢复 200 |
| **Android 打包** | 改了 `app/` 客户端且用户会装 APK，或用户刚说过要包/已约定「改完就包」；只跑 `node scripts/build-android.mjs` |

## 顺序

1. 必要测试（轻量：相关 `node --test` / `flutter test` 子集）
2. commit → push `main`（除非用户明确不要 push）
3. 若动了服务端 → 重启问象
4. 若动了客户端 → 打包到静态目录，并给出 ngrok 下载链接

## 例外

- 用户明确说「先别提交 / 别 push / 别重启 / 别打包」时遵守一次。
- 破坏性 git 操作、改全局配置、动其他项目：仍不做。
