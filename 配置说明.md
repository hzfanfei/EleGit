# 问象配置说明

只填仓库根目录一份 `.env`。服务和手机 App 都读它。填完再 `npm start` / `flutter run`。**不要把 `.env` 提交进 Git。**

先复制：

```bat
copy .env.example .env
notepad .env
```

## 每一项

| 变量 | 要填什么 |
|------|----------|
| `GITHUB_CLIENT_ID` | GitHub OAuth App 的 Client ID |
| `GITHUB_CLIENT_SECRET` | 同一 OAuth App 的 Client Secret（只留在这台电脑） |
| `WENXIANG_PUBLIC_URL` | 手机访问本机服务的公网根地址。默认已写已知 ngrok 主机。OAuth 回调是 `{该地址}/oauth/github/callback` |
| `WENXIANG_API_KEY` | 你自己定的一串密钥。服务校验请求头 `X-Wenxiang-Key`，App 构建时从 `.env` 注入，**手机里不用输入** |
| `WENXIANG_WORKSPACE` | 可选。选仓后克隆目录，默认 `~/问象`（Windows 为用户目录下的 `问象`），实际路径 `~/问象/<owner>/<repo>` |
| `WENXIANG_PORT` | 可选。本机服务端口，默认 `8787` |
| `CURSOR_API_KEY` / `CURSOR_AUTH_TOKEN` | 可选。本机 Cursor CLI 已 `agent login` 时可空。未登录时问答会回退本地进度适配器 |
| `VOLC_APP_ID` | 按住说话优先。火山 **新版** 豆包语音控制台的应用 APP ID |
| `VOLC_ACCESS_TOKEN` | 同上应用的 Access Token。与 APP ID 成对填写后即可按住说话（ASR） |
| `VOLC_API_KEY` | 可选。新版控制台若只发一张 API Key，填这项也能视为已配火山（常与下项 `VOLC_ASR_RESOURCE_ID` 一起填） |
| `VOLC_ASR_RESOURCE_ID` | **须与控制台开通的小时版套餐一致。** `volc.seedasr.sauc.duration`＝豆包流式语音识别 2.0 小时版（新版控制台常见）；`volc.bigasr.sauc.duration`＝较早的大模型流式 ASR 小时版。**未设置时代码默认 `volc.bigasr.sauc.duration`**，若你开的是 2.0 小时包必须在 `.env` 写 `volc.seedasr.sauc.duration`。见 [流式 ASR 2.0 文档](https://docs.volcengine.com/docs/6561/2630027?lang=zh) |
| `VOLC_TTS_VOICE` | 可选。合成音色，默认 `zh_female_vv_uranus_bigtts` |
| `VOLC_TTS_CLUSTER` | 可选。默认 `volcano_tts` |
| `OPENAI_API_KEY` | 没有火山密钥时的后备。走 OpenAI Realtime 听写 + 语音合成 |
| `OPENAI_REALTIME_MODEL` | 可选。默认 `gpt-4o-realtime-preview` |
| `OPENAI_TTS_VOICE` | 可选。默认 `alloy` |

问答走本机 Cursor ACP（`agent acp`，ask 模式），同一 `sessionId` 复用会话上下文。手机可新建 / 关闭会话。选仓后仍用本机 `git` 克隆到 `~/问象/<owner>/<repo>`。

**按住说话**：对话页麦克风按住说话 → 松手识别并自动发送。走 `/v1/voice/stt`（火山豆包流式 ASR，或后备 OpenAI）。**只需 ASR 密钥**，不必开通 TTS。没填语音密钥时 App 提示「还没配语音密钥」。App 无设置页，不显示厂商名。

**全屏豆包式电话**（听筒整通 ASR+TTS）默认关闭；仅调试时在 `.env` 设 `WENXIANG_VOICE_CALL_ENABLED=true` 并开通 TTS，见 `.env.example` 注释。

## 火山豆包语音（新版控制台）

入口：[新版语音概览 · projectName=default](https://console.volcengine.com/speech/new/overview?projectName=default)（旧 [speech/app](https://console.volcengine.com/speech/app) 常会跳转到新版 UI）。

1. **实名认证**：登录 [火山引擎控制台](https://console.volcengine.com/) 完成个人或企业实名。
2. **应用管理**：在新版语音控制台进入 **应用管理** → **创建应用**（项目可用 `default`）。
3. **开通 ASR**：为该应用开通 **豆包流式语音识别 / 大模型流式 ASR 2.0 / 小时版** 一类能力（名称以控制台为准）。**按住说话只要 ASR**；全屏电话才需要额外开通 TTS。
4. **复制凭证**：在应用详情复制 **APP ID**、**Access Token**（若只发 **API Key** 则填 `VOLC_API_KEY`，见上表）。勿提交到 Git。
5. **写入 `.env`**：
   - `VOLC_APP_ID`、`VOLC_ACCESS_TOKEN`，或仅 `VOLC_API_KEY`（见上表）
   - **`VOLC_ASR_RESOURCE_ID` 须与控制台小时版套餐一致**（2.0 小时版常用 `volc.seedasr.sauc.duration`；未设置时代码默认 `volc.bigasr.sauc.duration`）
6. **重启**：`server` 下 `npm start`，App 重新 `flutter run`（或 `sync-app-env.js` 后运行）。

服务端默认：ASR `wss://openspeech.bytedance.com/api/v3/sauc/bigmodel`，资源 ID 默认 `volc.bigasr.sauc.duration`（见 `server/src/voice-config.js`）。新版 2.0 小时包请在 `.env` 显式设置 `volc.seedasr.sauc.duration`。

Project 内更细的截图级步骤（若已同步）：`/cursor/stores/bc-d0755f26-a3ad-4de8-b86a-7c66b6eb5ff3/docs/wenxiang-volc-asr-setup.md`。

**按住说话（仅 ASR）示例**（控制台只给 API Key、且开通的是豆包流式语音识别 2.0 小时版时；占位符换成你自己的值，勿提交 `.env`）：

```env
VOLC_API_KEY=你的_API_Key
VOLC_ASR_RESOURCE_ID=volc.seedasr.sauc.duration
```

## GitHub OAuth App

1. 打开 [GitHub Developer settings → OAuth Apps](https://github.com/settings/developers)
2. Application name：问象
3. Homepage URL：与 `WENXIANG_PUBLIC_URL` 相同
4. Authorization callback URL：`{WENXIANG_PUBLIC_URL}/oauth/github/callback`

## 启动

```bat
cd server
npm install
npm start
```

```bat
cd app
flutter pub get
flutter run --dart-define-from-file=../.env
```

或先运行 `node scripts/sync-app-env.js`，再 `flutter run`（会生成已 gitignore 的 `app/lib/generated/env.g.dart`）。

App 打开后没有设置页：浏览器登录 GitHub → 选仓库 → 流式问答。
